import React, { useContext, useState } from "react";
import { AuthContext } from "../App";
import { postApi } from "../api";

// get으로 카페이름, 위치 받고 (필요한가?)
// 커피박 양 입력
// 언제 어디에 내놓을건지

const CafeApplyForm = () => {

    return (
        <div>
            CafeApplyForm
        </div>
    );
};

export default CafeApplyForm;
