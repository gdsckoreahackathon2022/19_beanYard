import React from "react";
import {ReactComponent as Logo} from '../assets/logo.svg';
import '../styles/Home.css';

const Home = () => {
    return (
        <div className="home">
            <Logo />
        </div>
    );
};

export default Home;
