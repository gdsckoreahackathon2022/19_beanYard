import React from "react";
import { LoginForm } from "../components";

const Login = () => {
    return (
        <div>
            <h2>LOG IN</h2>
            <LoginForm />
        </div>
    );
};

export default Login;
